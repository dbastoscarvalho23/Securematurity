{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app ID (from the Base44 dashboard)",
      "requiredAtBoot": true
    },
    {
      "name": "VITE_BASE44_APP_BASE_URL",
      "description": "Base44 backend URL for the SDK",
      "requiredAtBoot": true
    }
  ]
}
