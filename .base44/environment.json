{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app ID — found in the Base44 builder app settings",
      "requiredAtBoot": true
    },
    {
      "name": "VITE_BASE44_APP_BASE_URL",
      "description": "Base44 backend URL — found in the Base44 builder app settings",
      "requiredAtBoot": true
    }
  ]
}
